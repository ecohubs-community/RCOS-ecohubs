---
import '../styles/global.css';
import Sidebar from '../components/Sidebar.svelte';
import { buildLayerNav } from '../lib/layerNav';

interface Props {
  title?: string;
  description?: string;
}

const { title = 'EcoHubs', description = 'EcoHubs community RCOS documentation.' } = Astro.props;
const fullTitle = title === 'EcoHubs' ? title : `${title} · EcoHubs`;
const pathname = Astro.url.pathname.replace(/\/$/, '') || '/';
const layerNav = await buildLayerNav();
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <title>{fullTitle}</title>
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
  </head>
  <body>
    <div class="shell">
      <Sidebar client:load pathname={pathname} layers={layerNav} />
      <main class="main">
        <div class="main-inner">
          <slot />
        </div>
      </main>
    </div>
  </body>
</html>

<style>
  .shell {
    display: flex;
    align-items: flex-start;
    min-height: 100dvh;
  }
  /* Let the Svelte sidebar island's children participate directly in the
     shell flex container so the mobile top-bar can span full width. */
  .shell > :global(astro-island) {
    display: contents;
  }
  .main {
    flex: 1;
    min-width: 0;
  }
  .main-inner {
    max-width: 62rem;
    margin: 0 auto;
    padding: 3rem 2.5rem 5rem;
  }
  @media (max-width: 768px) {
    .shell {
      flex-direction: column;
      align-items: stretch;
    }
    .main {
      width: 100%;
      min-width: 0;
    }
    .main-inner {
      padding: 1.5rem 1.25rem 3rem;
      min-width: 0;
    }
  }
  @media (max-width: 480px) {
    .main-inner {
      padding: 1.25rem 1rem 2.5rem;
    }
  }
</style>
